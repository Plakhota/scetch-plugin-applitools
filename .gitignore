reference-archive/
old-sketch-plugin/
figma-plugin-applitools/

node_modules/
dist/
*.sketchplugin
resources/
.DS_Store
.claude/

# Built distributable zip — regenerate via `npm run build` + zipping
# applitools.sketchplugin (see artifacts/INSTALL.md); not meant to be
# versioned, since it's a binary build output that changes every rebuild.
artifacts/*.zip